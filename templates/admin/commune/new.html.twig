{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_admin_commune_new' ? 'action.add' : 'action.edit' %}

{% block title %}{{ action|trans }} {% trans %}d'une commune{% endtrans %}{% endblock %}

{% block body %}
    <div class="container formulaire-ajout">
        <div class="py-5">
            <h1>{{ action|trans }} {% trans %}d'une commune{% endtrans %}</h1>
        </div>

        {{ include('admin/commune/_form.html.twig') }}
        
        <div class="py-5">
            <a class="btn btn-warning" href="{{ path('app_commune_index') }}">{% trans %}Annuler{% endtrans %}</a>
        </div>
    </div>
{% endblock %}
