<form method="post" action="{{ path('app_admin_eveche_delete', {'id': eveche.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ eveche.id) }}">
    <button class="btn">{% trans %}Supprimer{% endtrans %}</button>
</form>
